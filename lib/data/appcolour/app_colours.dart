import 'dart:ui';

import 'package:flutter/material.dart';

class AppColors {
  static const Color primary =
      Color(0xFF2C3E50); // Dark Blue-Grey (Deep, Serious)
  static const Color secondary =
      Color(0xFF1ABC9C); // Muted Teal (Refreshing, Motivating)
  static const Color highlight =
      Color(0xFFF39C12); // Warm Amber (Subtle Attention)
  static const Color background =
      Color(0xFFECF0F1); // Soft Light Grey (Modern, Minimal)
  static const Color cardBackground = Color(0xFFFFFFFF); // White (Clean Look)
  static const Color text = Color(0xFF2C3E50); // Dark Blue-Grey (Serious)
  static const Color textLight =
      Color(0xFFBDC3C7); // Muted Grey (Less Intense Contrast)
  static const Color blue = Color(0xFF34495E); // Muted Navy Blue (Trustworthy)
  static const Color green =
      Color(0xFF27AE60); // Deep Green (Professional, Balanced)
  static const Color red =
      Color(0xFFC0392B); // Muted Red (Alerting but not Aggressive)
  static const Color orange =
      Color(0xFFD35400); // Deep Orange (Strong, Not Playful)
}

const supabaseUrl = '';
const supabaseKey = String.fromEnvironment(
  '',
);
